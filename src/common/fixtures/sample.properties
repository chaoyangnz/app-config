rate=1
comment=b
percentage=12.6
dbName=${DB_NAME}
jdbcUrl=jdbc://${DB_HOME}:${DB_PORT}/${DB_NAME}
maxWidth=1234
database.timeout=1
database.name=db
database.utility=12.6
database.url=jdbc://${DB_HOME}:${DB_PORT}/${DB_NAME}
